name: flutter_bullet_list
description: A Flutter package for web-like ordered and unordered lists with multi-level items and customizable bullet styles.
version: 0.0.4
homepage: https://github.com/R3-V3NG3R/flutter_bullet_list

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  numerus: ^1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

